# Doc de-rot pass — fix plan for the 2026-07-11 documentation audit

Status: **executed 2026-07-11** — all edits applied; commits proposed and
awaiting the owner's word. Docs-only; zero code, zero schema, zero deps. Source: the 2026-07-11 audit (delivered in chat; findings
restated inline below so this spec is self-contained).

**Ground rules for the executor:**

- Fix on the current branch `codex/password-reset` (see "Where the fixes land"
  below). Docs-only commits, grouped as in §6; commit only on the owner's word.
- Respect each doc's contract: `progress-log.md` is append-only — corrections are
  **bracketed annotations**, never rewrites. Dated audit artifacts
  (`CODE_AUDIT_2026-06-11.md`, `UI_AUDIT_2026-06-11.md`) are never edited.
- Match each doc's existing prose style. Do not "improve" passages the audit did
  not flag.
- Batch-Read every target file before the sweep (global rule).
- Verify claims you write the same way the audit verified them: against git and
  the working tree, not against another doc's restatement.

## Where the fixes land

The freshest docs live on `codex/password-reset` (they include both the 2026-07-08
UX-fixes wrap from `main` and the M11 wrap `b92db61`). Fixing there means the
corrections ride into `main` with the M11 merge. Tradeoff: if M11 stalls on its
owner gates, `main`'s docs stay rotten in the meantime — acceptable, since the
next session's canon is read from this branch anyway. Fallback if M11 stalls
long-term: cherry-pick the docs commits onto a `codex/doc-derot` branch off
`main` (docs-only may go direct to `main` after review, per CLAUDE.md).

## Verified reality (write TO this, not from memory)

- `main` = `6fb32b2` (merge of PR #36, post-use UX fixes, 2026-07-08), local and
  `origin/main` in sync.
- `codex/password-reset` = `b92db61` ← `e2b7ea5` ← `391ebe1` (M11 feat/test/docs,
  author-dated 2026-07-06), **rebased onto `6fb32b2`**, working tree clean,
  **unpushed** (no upstream). Backup ref `codex/password-reset-prerebase` exists.
- M11 remaining gates (owner-side): AR2 board sign-off · Supabase-dashboard
  redirect URLs · then **push/PR/merge** on the word · post-deploy prod
  real-device pass. ("Commit" is no longer a step — it happened.)
- vitest 138/138 across 9 files (re-run 2026-07-11); pgTAP 3 suites / 108
  assertions; schema.sql 699 lines; 5 files in `supabase/migrations/`; the
  `rgba(31, 35, 31, 0.04)` literal sits at `app/globals.css:1911`.
- Local Supabase stack is **down** (Colima/Docker daemon not running) as of
  2026-07-11.

## 1. Pass 1 — current-state.md (the resume block; highest consequence)

All edits keep the 2026-07-08 top line's framing (it is the one already-correct
statement: "committed there, unpushed/unmerged").

- **§Current build phase (line ~10):** "nothing is committed or merged" →
  committed on `codex/password-reset` as 3 commits (`391ebe1` feat, `e2b7ea5`
  test, `b92db61` docs wrap), rebased onto `main` `6fb32b2`, unpushed/unmerged,
  awaiting owner gates.
- **§Stable Baseline, In-flight bullet (~106-111):** replace the
  "uncommitted working-tree changes … not committed, not merged" description
  with the committed/rebased/unpushed state. The file list stays (it is now the
  commit contents, not working-tree changes).
- **§Stable Baseline, `main` bullet (~112):** `main` is at `6fb32b2` (**PR #36,
  post-use UX fixes**: `97e6c57` feat + `1a62a9b` docs wrap) atop `cc1e6ec` …
  (keep the existing chain below unchanged). "origin/main and local main in
  sync" now refers to `6fb32b2`.
- **§Active Handoff, Prior (2026-07-06) entry (~266):** annotate that the build
  was subsequently committed (3 commits) and the branch rebased onto `6fb32b2`.
- **§Active Handoff, Next action (~330-347):** drop "the branch has uncommitted
  working-tree changes (nothing committed yet)". Renumber the close-out
  sequence: 1. AR2 sign-off → 2. owner Supabase redirect URLs (STOP, owner-run)
  → 3. **push branch / open PR as `mitchthompson` / merge on the word** (commit
  step deleted — done) → 4. prod real-device pass.
- **§Environment notes (~385-420):** working branch "(M11, uncommitted +
  unmerged)" → "(M11, committed + rebased onto `6fb32b2`, unpushed)"; "in sync
  at `cc1e6ec`" → "in sync at `6fb32b2`"; the "Currently left up (2026-07-06)
  running WITH mailpit" stack note → note the stack was found **down** on
  2026-07-11 (Colima not running); keep the restart-with-mailpit instructions.
- **§Milestone status table, row 11 (~462):** "(uncommitted, unmerged)" →
  "(committed on branch, unpushed/unmerged; owner gates open)".
- Bump "Last reviewed" to 2026-07-11 and name this pass as the reason.

## 2. Pass 2 — the other canon registers (same root cause)

- **roadmap.md §11 (~372):** "uncommitted + unmerged" → committed (3 commits,
  rebased onto `6fb32b2`), unpushed/unmerged; gates unchanged.
- **design-flags.md, auth-flow flag (~36):** "(uncommitted, unmerged)" →
  "(committed on `codex/password-reset`, unpushed/unmerged)". While in the file:
  fix the rgba literal's cited location "~line 1809" → "~line 1911" (Open flag
  "One color literal evades the hex-grep guard", ~10-11).
- **progress-log.md, 2026-07-06 entry (~47-53):** append-only — add a bracketed
  annotation directly under the entry heading, e.g.:
  `> [Update 2026-07-11: this work was committed the same day as 391ebe1 / e2b7ea5 / b92db61 and the branch later rebased onto main 6fb32b2 (post-PR #36); "uncommitted" below describes the state mid-session only.]`
  Do not edit the entry body. (No new dated entry: this plan's own wrap entry
  will cover the doc pass when it lands.)

## 3. Pass 3 — onboard skill (one small fix, big blast radius)

- **`.claude/skills/onboard/SKILL.md` (~44-46):** delete "`npm run lint` is
  non-functional (no ESLint config — known flag; do not 'fix' it in passing)."
  — stale since PR #23 (2026-07-03): `npm run lint` = `eslint .
  --max-warnings=0` on the flat config, CI-enforced. Add `npm run lint` to the
  step-3 baseline command (`npm run lint && npm run typecheck && npm run test`),
  keeping the ~20s framing honest.

## 4. Pass 4 — reference-doc de-rot (one commit, four docs)

### architecture.md (worst single-doc rot; required reading for DB work)

- **§Current Data Flows (55-74):** rewrite all three flows to post-M2/M3/M4
  reality — recipe save via the atomic `save_recipe` RPC (security invoker,
  diff-based version bump); plan version bumps via grocery-scoped DB triggers
  (client performs no version writes); grocery regeneration via transactional
  `regenerate_grocery_list` upsert-by-identity preserving checked/on-hand/pantry
  state. Crib the wording from current-state's Architecture snapshot and the
  2026-07-02 decisions entries; do not invent new claims.
- **§Known Structural Debt (121-127):** drop "aggregate writes are composed in
  the browser" and "display-date formatting remains duplicated" (both closed).
  Keep "generated Supabase database types are not present"; soften the
  route-file line to match the post-M6 hook extraction.
- **§Test Boundaries (129-144):** state present reality: vitest 138 across 9
  files (lib domain logic incl. errors + import pipeline), pgTAP 108 across
  three suites in CI since 2026-07-01; UI-interaction coverage still open
  (Playwright harnesses in `scripts/review-board/` are per-change verify
  passes, not CI).
- **§Applying Database Migrations (~172):** "There is no Supabase CLI
  installed" → align with the doc's own Tooling Status: CLI installed for
  local/CI testing only; prod stays hand-applied via the SQL editor.
- **§Vercel Deploy (~198-201):** replace the "Flagged assumption (unconfirmed)"
  block with the confirmed trigger (design-flags Resolved, 2026-07-01): merge to
  `main` auto-deploys production; branch pushes create previews; no manual
  promote. Keep the release-approval framing.
- **§Tooling Status (~219):** "GitHub CLI is not installed" → gh is installed
  and holds both accounts; PRs are driven per-command as `mitchthompson` via
  `GH_TOKEN=$(gh auth token --user mitchthompson)`.
- Minor: "meal-slot scheduling" (~24) and "for a date and meal type" (~47-48) →
  flat-day language, `meal_type` vestigial.

### routes.md

- `/recipes` row: drop the "Load sample data" seeder (removed PR #21, RC3);
  mention the Import button / `?import=1` flow instead.
- `/plans` row: quick-add now opens the full-screen takeover
  (`components/plan-add-meal.tsx`); reads `?plan=<id>` / `?new=1` deep links;
  exit is "Shop this plan" → `/grocery?plan=<id>`.
- `/grocery` row: replace "auto-regeneration on staleness + manual Regenerate"
  with the amber staleness banner + explicit Generate/Update button (M10 PR 1);
  reads `?plan=<id>`.
- **Add a `/reset-password` row** (`app/reset-password/page.tsx` + `layout.tsx`,
  M11): recovery-session password form; note it ships with the unmerged M11
  branch if the row lands before merge.
- §Notes (~52): drop the resolved `ensureUserSettings`-twice and
  defaults-disagree sentence (both closed; see design-flags Resolved).
- §Shared layout: add the "Forgot password?" link to the AuthGate description
  (one clause, no more).

### design-system.md (CSS is canonical; describe what globals.css contains)

- **Plan components (~255-276):** quick-add description → the full-screen
  takeover; document the `.plan-add-*` selector group (globals.css ~756-800:
  `.plan-add-meal`, `.plan-add-head`, `.plan-add-close`, `.plan-add-day`,
  `.plan-add-body`) in the section's existing bullet style, verified against the
  CSS, not from memory. `.plan-generate` bullet: the exit is "Shop this plan" →
  `/grocery?plan=<id>`; delete "(Shop regenerates from staleness on load)" —
  this also resolves the internal contradiction with the Shop section's
  correct "nothing regenerates on load" (~300).

### pages/settings.md

- §Known flags (~66-74): both flags are resolved — either delete them or move
  them to a short "Resolved" note pointing at design-flags. §States "Empty / no
  row" (~57-60): defaults now come from `DEFAULT_USER_SETTINGS`
  (`lib/constants.ts`, mirrors SQL: plan_days 7, week_starts_on 5, order/pickup
  **null**) — confirm against the file before writing.

## 5. Pass 5 — small files (same commit as Pass 4 is fine)

- **product.md:** workflow step 3 → flat-day language ("Add cooked meals,
  leftovers, or eating-out entries to plan days"); §Current Routes "`/`:
  Dashboard" → Today (reflow home).
- **root README.md:** features bullet → "Custom date-range meal plans with flat
  per-day meal lists (cooked meals, leftovers, eating-out)".
- **docs/README.md:** add `redesign-brief.md` (Design section) and a
  `plans/` entry (State and planning: "builder-ready milestone specs; a spec's
  Status header is current-state, the rest is intent") to the Documents index.
  Optionally name `mockups/` and `assets/` under a one-line "artifacts" note.
- **setup.md:** "(13 as of the last run)" → "(138 as of 2026-07-11)" or drop the
  count (recommended: drop — counts rot); "(no Supabase CLI installed)" → CLI
  used for local/CI only; delete the "(Confirm the exact deploy trigger — open
  item)" parenthetical (resolved 2026-07-01); ".claude/ folder is gitignored" →
  gitignored **except** `.claude/skills/` (onboard + wrap travel with the repo).
- **plans/ipad-support.md Status header:** → shipped — chrome PR #26
  (`e0a6a3c`) + content-width PR #27 (`a40b90a`) merged & deployed 2026-07-03,
  owner-confirmed on iPad Pro; only the optional real-device tails from
  current-state's Open issues remain.

## Deliberately NOT fixed

- `pages/today.md`, `pages/plans.md`, `pages/grocery.md` — acknowledged stubs;
  the "Per-page docs are stubs" flag already disclaims them and the redesign
  brief supersedes their intent. Fleshing them out is real (unscheduled) work,
  not de-rot. Optional cheap middle ground if the owner wants it: one
  "Stub — superseded by redesign-brief.md; behavior claims may predate M10/UX
  fixes" line at the top of each.
- `redesign-brief.md` ~119 ("staleness-driven auto-regeneration (shipped
  default…)") — intent doc, historically accurate, and it explicitly
  anticipated the M10 change ("drop it once trusted"). Leave.
- Dated audits, `mockups/`, `assets/` — artifacts.
- All Unverifiable claims (Vercel/prod/CI-green/board verdicts) — nothing
  suggests they're wrong; they're just not provable offline. Owner spot-check:
  one `gh run list` + one browser hit on `meal-queue.vercel.app`.

## 6. Commit plan (Conventional Commits, no trailers; owner's word first)

1. `docs: de-rot handoff canon after M11 commit + rebase (audit 2026-07-11)` —
   current-state.md, roadmap.md, design-flags.md, progress-log.md annotation
   (Passes 1-2).
2. `chore(skills): onboard — lint is functional since PR #23` — Pass 3.
3. `docs: sweep stale reference docs (architecture, routes, design-system, product, setup, READMEs, settings page doc, ipad status)` —
   Passes 4-5, plus this plan file itself.

## 7. Verification (docs-only, per qa.md)

- Diff review per commit; internal links resolve (lowercase-kebab names).
- Re-run the audit's rot greps — all must come back empty:
  - `/usr/bin/grep -rn "uncommitted" docs/current-state.md docs/roadmap.md docs/design-flags.md` (progress-log keeps its annotated historical use)
  - `/usr/bin/grep -n "cc1e6ec" docs/current-state.md` — hits only historical
    chain references ("atop `cc1e6ec`"), never "main is at" / "in sync at"
  - `/usr/bin/grep -rn -i "load sample\|regenerat.*on load\|runs twice\|no eslint\|non-functional" docs/routes.md docs/design-system.md docs/architecture.md docs/pages/settings.md .claude/skills/onboard/SKILL.md`
  - `/usr/bin/grep -n "lunch and dinner\|Dashboard" docs/product.md README.md`
  - `/usr/bin/grep -n "awaiting merge" docs/plans/ipad-support.md`
- Spot-check every git hash written into a doc against `git log` before commit.
- No lint/typecheck/test required (no code touched) — but nothing stops a
  paranoia run; commit 2 touches only a tracked markdown skill file.

## Rough size

~30-40 focused edits across 14 files; one session. The wrap for that session
adds the usual progress-log entry and bumps current-state's "Last reviewed".
