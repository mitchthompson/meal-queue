NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only (no NEXT_PUBLIC_ prefix). Recipe import (POST /api/import-recipe)
# calls Claude Haiku 4.5. Optional: the app builds and runs without it, and the
# route returns a not_configured error until it is set. Setup: docs/plans/recipe-import.md section 8.
ANTHROPIC_API_KEY=
